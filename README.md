# SIMPLE PRINT SERVER

### Description
Mock Print Server

### Installation

$ pnpm install


### Running the app
서버는 `2000` port 에서 실행 됩니다. main.ts `app.listen(2000);`
```bash
$ pnpm run start:dev
```
